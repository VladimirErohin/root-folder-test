import React from 'react';
import h from './Header.module.css';

const Header = () => {
    return (
        <div className={h.header}>
            <h2>Simple tree</h2>
        </div>
    );
};

export default Header;